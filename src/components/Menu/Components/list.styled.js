import styled from 'styled-components';

export const List = styled.ul`
  height: 0;
  list-style: none;
  overflow: hidden;
  opacity: 0;
  margin: 0;
  padding: 10px 10px;
  transition: opacity 300ms ease-out;
`;
